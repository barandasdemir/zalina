<%- include partials/header %>

<div class="container cartwrapper">
    <br>
    <h5 class="center"><b>ALIŞVERİŞ SEPETİ</b></h5>
    <br>

    <div class="row">
        <div class="col s8 m8 cart">
            <% if (session.cart && session.cart.length > 0) { %>
            <% session.cart.forEach((item, index) => { %>
            <% total += item.totalPrice %>
            <div id="<%= item.id %>" class="card horizontal">
                <div class="card-image">
                    <img src="https://via.placeholder.com/150x190.jpg?text=insert+image+here">
                </div>
                <div class="card-stacked">
                    <div class="card-content">
                        <button class="btn z-depth-0 button-rm right white red-text" style="cursor:pointer">X</button>
                        <h6>
                            <b>
                                <%= item.name %>
                                <br>
                                <%= item.price.toFixed(2) %> ₺
                            </b>
                        </h6>
                        <br>
                        <p>Ürün kodu:
                            <%= item.id %>
                        </p>
                        <p>Beden: <%= item.size %></p>
                        <p>Renk: <%= item.color %></p>
                        <p class="productTotal">
                            Toplam:
                            <%= item.totalPrice.toFixed(2) %> ₺
                        </p>
                        <div class="row valign-wrapper right">
                            <a class="waves-effect waves-light btn qtyMinus white black-text z-depth-0">-</a>
                            <div id="itemQty">
                                <%= item.qty %>
                            </div>
                            <a class="waves-effect waves-light btn qtyPlus white black-text z-depth-0">+</a>
                        </div>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
        <ul class="collection col s12 m4 cart">
            <p class="center">Alışveriş sepeti toplamı</p>

            <li class="collection-item total">
                Sipariş değeri
                <div class="right">
                    <%= total.toFixed(2) %> ₺
                </div>
            </li>
            <li class="collection-item total">
                Toplam
                <div class="right">
                    <%= total.toFixed(2) %> ₺
                </div>
            </li>
            <li class="collection-item total">
                WIP<br>
                refresh to update for now
            </li>
            <br>
            <a href="/cart/checkout" class="waves-effect waves-light btn black col s12" style="margin-bottom:1em">Siparişi tamamla</a>
        </ul>
    </div>
    <% } else { %>
    <h6><b>Alışveriş sepetiniz boş!</b></h6>
    <p>Alışveriş sepetinize ürün kaydetmek veya daha önce kaydedilmiş ürünlere erişmek için oturum açın.</p>
    <% } %>
</div>
</div>
</div>

<%- include partials/footer %>
<script src="/javascripts/cart.js"></script>
